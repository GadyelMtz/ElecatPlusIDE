programa #luces#{

	#luces#(){
		led #ledIzquierda# = 8;
		led #ledDerecha# = 9;
	}

	ejecutar(){
		accion(#ledIzquierda#, encender());
		esperar(1500);
		accion(#ledIzquierda#, apagar());
		esperar(1500);
	}
	
	
	
}
